version: '3'

services:
  htmlparser-backend:
    build: .
    container_name: htmlparser-backend
    volumes:
      - ../:/html-parser-backend
    command: bash
    tty: true
    ports:
      - 8787:8787